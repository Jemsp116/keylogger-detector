# Byte-compiled / cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Scan reports (the committed example under examples/ is kept intentionally)
/report.json
*.report.json

# Environments / editors
venv/
.venv/
.env
.idea/
.vscode/

# Local tool settings
.claude/settings.local.json

# PyInstaller build artifacts (see build.py). The exe is published as a
# GitHub Release asset, never committed to the repo.
build/
dist/
build_resources/
*.spec
*.manifest
version_info.txt
*.sha256
